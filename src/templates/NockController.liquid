
{%- if HasOperations -%}
{%- assign NockClassName = Class | prepend: 'Qqqqqq' | slice: 0, Class.size | append: "Nock" | slice: 6, 1000 -%}

{%- for operation in Operations -%}
{%-   if operation.Parameters.size > 0 -%}

{%- assign firstLetter = operation.ActualOperationName | slice: 0, 1 | upcase -%}
{%- assign parameterClassName = operation.ActualOperationName | append: 'q' | slice: 1, operation.ActualOperationName.size | prepend: firstLetter | slice: 0, operation.ActualOperationName.size | append: NockClassName | append: 'Parameters' -%}
type {{ parameterClassName }} = {
  {%- for parameter in operation.Parameters -%}
    {%- if parameter.Type != 'FileParameter' -%}
  {{ parameter.VariableName }}{% if GenerateOptionalParameters and parameter.IsOptional %}?{% endif %}: {{ parameter.Type }}{{ parameter.TypePostfix }};
    {%- endif -%}
  {%- endfor -%}
};

{%-   endif -%}
{%- endfor -%}

export const {{ NockClassName }} = {
{%- for operation in Operations -%}
{%- if operation.HttpMethodUpper == 'Get' or operation.HttpMethodUpper == 'Post' or operation.HttpMethodUpper == 'Put' -%}
  {%- assign ResultType = operation.ResultType -%}
  {%- assign firstLetter = operation.ActualOperationName | slice: 0, 1 | upcase -%}
  {%- assign parameterClassName = operation.ActualOperationName | append: 'q' | slice: 1, operation.ActualOperationName.size | prepend: firstLetter | slice: 0, operation.ActualOperationName.size | append: NockClassName | append: 'Parameters' -%}

  {% if operation.HttpMethodUpper == 'Get' %}
  {{ operation.ActualOperationName }}: ({%- if operation.Parameters.size > 0 -%}queryParams: {{ parameterClassName }}, {% endif %}requestBody?: RequestBodyMatcher, interceptorOptions?: Options) => {
    {% template Path %}
    return nock(getBaseUrl())
      .get(url_, requestBody, interceptorOptions)
      {% if operation.Parameters.size > 0 -%}.query(queryParams){% endif %};
  },
  {% elsif operation.HttpMethodUpper == 'Post' %}
  {{ operation.ActualOperationName }}: ({%- if operation.Parameters.size > 0 -%}queryParams: {{ parameterClassName }}, {% endif %}requestBody?: RequestBodyMatcher, interceptorOptions?: Options) => {
    {% template Path %}
    return nock(getBaseUrl())
      .post(url_, requestBody, interceptorOptions);
  },
  {% elsif operation.HttpMethodUpper == 'Put' %}
  {{ operation.ActualOperationName }}: ({%- if operation.Parameters.size > 0 -%}queryParams: {{ parameterClassName }}, {% endif %}requestBody?: RequestBodyMatcher, interceptorOptions?: Options) => {
    {% template Path %}
    return nock(getBaseUrl())
      .put(url_, requestBody, interceptorOptions);
    },
  {% elsif operation.HttpMethodUpper == 'Patch' %}
    {{ operation.ActualOperationName }}: ({%- if operation.Parameters.size > 0 -%}queryParams: {{ parameterClassName }}, {% endif %}requestBody?: RequestBodyMatcher, interceptorOptions?: Options) => {
    {% template Path %}
    return nock(getBaseUrl())
      .patch(url_, requestBody, interceptorOptions);
    },
  {% elsif operation.HttpMethodUpper == 'Delete' %}
    {{ operation.ActualOperationName }}: ({%- if operation.Parameters.size > 0 -%}queryParams: {{ parameterClassName }}, {% endif %}requestBody?: RequestBodyMatcher, interceptorOptions?: Options) => {
    {% template Path %}
    return nock(getBaseUrl())
      .delete(url_, requestBody, interceptorOptions);
    },
  {% endif %}

  {% if ResultType != 'void' %}
  {{ operation.ActualOperationName }}Reply: ({%- if operation.Parameters.size > 0 -%}queryParams: {{ parameterClassName }}, {% endif %}response: {{ ResultType }}, requestBody?: RequestBodyMatcher, interceptorOptions?: Options) => {
    return {{ NockClassName }}.{{ operation.ActualOperationName }}({%- if operation.Parameters.size > 0 -%}queryParams, {% endif %}requestBody, interceptorOptions).reply(200, response);
  },
  {% endif %}
{% endif -%}
{% endfor -%}
}
{% endif -%}
