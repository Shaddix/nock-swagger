{%- if operation.Parameters.size > 0 -%}
const {  {% for parameter in operation.QueryParameters %}{{ parameter.VariableName }},{% endfor %}{% for parameter in operation.PathParameters %}{{ parameter.VariableName }},{% endfor %}} = queryParams;
{% endif %}
const getUrl = (function (this: { baseUrl: string }) {
  {% template Client.RequestUrl %}
  return url_;
}).bind({ baseUrl: '' });
const url = getUrl();
